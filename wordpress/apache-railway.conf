ServerName localhost
# Railway's edge sets X-Real-IP to the connecting client (X-Forwarded-For can carry an extra internal hop
# after it). Trust it only from Railway's proxy range, 100.64.0.0/10.
RemoteIPHeader X-Real-IP
RemoteIPInternalProxy 100.64.0.0/10
